import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  subHeadingSection: {
    color: 'rgba(210,134,147, .9)',
    textAlign: 'center'
  },
  heading: {
    fontWeight: 700
  }
}));
