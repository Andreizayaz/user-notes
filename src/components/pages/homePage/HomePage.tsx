import { FC, ReactElement } from 'react';

import { MainContainer } from 'src/components/common';

export const HomePage: FC = (): ReactElement => (
  <MainContainer title='User Notes'>Home Page</MainContainer>
);
